import * as http from './http.js'

function loadAnimations() {
    console.log('loading all animations')

    let animations = document.getElementById('animations')

    // call backend and load all animaitons
    
    // create element for each animation
}

loadAnimations()